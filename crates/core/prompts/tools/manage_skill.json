{
  "name": "manage_skill",
  "description": "List, load, activate, inspect available skills and their bundled resources, execute a declared script resource helper through the skill resource helper sandbox, and create, inspect, apply, or reject controlled skill change proposals. When a skill from the Available Skills metadata index clearly matches the user task, call activate_skill before answering or following that workflow. Use view_skill only to inspect a skill without committing to its workflow. Use list_skills when no relevant indexed skill is shown but the user asks for a reusable skill. Use view_resource when a loaded skill references a bundled file. Use run_resource_helper only when a loaded skill explicitly instructs you to run one of its script resources. Use proposals for self-evolution when a repeated workflow lesson should become a reusable skill. Proposals are safety-scanned; applying a proposal mutates active user skills and requires confirmation.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "list_skills",
          "view_skill",
          "activate_skill",
          "view_resource",
          "run_resource_helper",
          "propose_create",
          "propose_patch",
          "list_proposals",
          "view_proposal",
          "apply_proposal",
          "reject_proposal"
        ],
        "description": "Skill inspection, activation, resource loading/helper execution, or evolution action to perform."
      },
      "proposal_id": {
        "type": "string",
        "description": "Proposal id for view_proposal, apply_proposal, or reject_proposal."
      },
      "skill_id": {
        "type": "string",
        "description": "Skill id for view_skill, activate_skill, view_resource, run_resource_helper, or existing user skill id for propose_patch. Built-in skill ids are read-only for patching."
      },
      "resource_path": {
        "type": "string",
        "description": "Bundled resource path for view_resource or run_resource_helper, for example references/playbook.md or scripts/helper.py. run_resource_helper only supports script resources ending in .py, .js, .mjs, or .cjs."
      },
      "helper_args": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Arguments passed to the skill resource helper script after the script path when action is run_resource_helper."
      },
      "stdin": {
        "type": "string",
        "description": "Optional stdin payload for run_resource_helper. Use this for large input instead of stuffing it into helper_args."
      },
      "timeout_secs": {
        "type": "integer",
        "minimum": 1,
        "maximum": 300,
        "description": "Optional run_resource_helper timeout in seconds. Defaults to 30."
      },
      "name": {
        "type": "string",
        "description": "Skill name for propose_create, or replacement name for propose_patch."
      },
      "description": {
        "type": "string",
        "description": "Concise trigger description for the proposed skill."
      },
      "content": {
        "type": "string",
        "description": "Full proposed skill body. Keep it procedural, specific, and reusable. Large valid SKILL.md bodies are accepted; use resource_bundle for scripts, references, and assets instead of flattening them into this field."
      },
      "resource_bundle": {
        "type": "array",
        "description": "Optional complete resource bundle for propose_create or propose_patch. Paths must be relative and normally begin with scripts/, references/, assets/, or agents/.",
        "items": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string"
            },
            "kind": {
              "type": "string",
              "enum": ["script", "reference", "metadata", "asset"]
            },
            "encoding": {
              "type": "string",
              "enum": ["utf8", "base64"]
            },
            "content": {
              "type": "string"
            }
          },
          "required": ["path", "kind", "encoding", "content"],
          "additionalProperties": false
        }
      },
      "rationale": {
        "type": "string",
        "description": "Why this skill change is warranted, grounded in observed repeated behavior or user feedback."
      },
      "status": {
        "type": "string",
        "enum": ["pending", "applied", "rejected"],
        "description": "Optional proposal status filter for list_proposals."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100,
        "description": "Maximum skills or proposals to return for list_skills or list_proposals."
      }
    },
    "required": ["action"]
  }
}
