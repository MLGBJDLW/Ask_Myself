{
  "name": "tool_search",
  "description": "Search the enabled built-in and MCP tool catalog by name and description. Use this when the right tool is hidden by dynamic visibility or when you are unsure which local tool fits a task. Matching hidden tools are activated for the next model step; disabled MCP servers are not discoverable until connected.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Natural language query or tool-name fragment"
      },
      "limit": {
        "type": "integer",
        "description": "Maximum matches to return (default 8, max 20)",
        "minimum": 1,
        "maximum": 20
      }
    },
    "required": ["query"]
  }
}
