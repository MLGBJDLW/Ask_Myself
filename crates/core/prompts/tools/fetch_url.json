{
  "name": "fetch_url",
  "description": "Fetch and read the text content of a web page with SSRF and redirect-hop validation. Use this after web_search or when the user shares a URL. Returns extracted text plus URL/title provenance and a suggested citation.",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to fetch (must be http:// or https://)."
      },
      "max_length": {
        "type": "integer",
        "description": "Maximum number of characters to return. Default: 5000."
      }
    },
    "required": ["url"]
  }
}
