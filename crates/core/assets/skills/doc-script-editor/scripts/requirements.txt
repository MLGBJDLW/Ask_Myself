# Human-reviewed direct dependency inputs. Runtime installation uses
# requirements.lock with --require-hashes; update both files together.
python-docx==1.2.0
python-pptx==1.0.2
pypdf==6.10.0
openpyxl==3.1.5
pywin32==312; sys_platform == "win32"
