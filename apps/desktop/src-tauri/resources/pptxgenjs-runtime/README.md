Release builds populate this directory with a platform Node executable, the
reviewed PptxGenJS dependency closure, and a per-file SHA-256 runtime manifest.
Generated runtime files are intentionally excluded from source control.
