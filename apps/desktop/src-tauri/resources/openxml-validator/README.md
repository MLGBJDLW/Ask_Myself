# Open XML validator runtime

Release builds publish the pinned `tools/openxml-validator` project as a
self-contained single-file binary into this directory before Tauri packaging.
Source checkouts intentionally do not commit generated executables.
